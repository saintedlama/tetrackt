# Stable tracker synth integration

> Status: **TODO**

When switching tracks or instruments, the current implementation of the tracker component resets to a default state, losing any customizations or settings applied to the synthesizer. To provide a more seamless and user-friendly experience, we should implement a stable integration between the tracker and the synthesizer components.
