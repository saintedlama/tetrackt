# Tracker Synth Screens

To allow for more flexibility in the UI, including display of additional information such as the current BPM, we should consider implementing separate screens for the tracker and the synthesizer. This would allow us to display more information and controls without cluttering the main tracker interface.

Basically splitting instrument and pattern editor into separate screens.
